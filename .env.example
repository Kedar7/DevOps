# Example environment variables for the Flask app
# Set MONGODB_URI to your MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/myFirstDatabase?retryWrites=true&w=majority"
# Optionally set the database name (otherwise the DB in the URI or 'test' is used)
MONGODB_DB="mydatabase"
